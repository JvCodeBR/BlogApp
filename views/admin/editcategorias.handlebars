{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{/each}}

<h1>Editar categoria</h1>
<div class="card">
    <div class="card-body">
        <form action="/admin/categorias/edit" method="POST">
            <input type="hidden" name="id" id="id" value="{{categoria._id}}">
            <label for="nome" class="form-label">Nome</label>
            <input type="text" class="form-control" id="nome" name="nome" placeholder="Nome da categoria" value="{{categoria.nome}}">
            <label for="slug" class="form-label">Slug</label>
            <input type="text" class="form-control" id="slug" name="slug" placeholder="Slug da categoria" value="{{categoria.slug}}">
            <button type="submit" class="btn btn-success mt-4">Editar categoria</button>
        </form>
    </div>
</div>

